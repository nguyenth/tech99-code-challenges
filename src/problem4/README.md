# Problem 4 — Three ways to sum to n

Description
- Implementation for the "three ways to sum to n" challenge. The code under this folder contains the solution and unit tests that verify correctness.

Prerequisites
- Node.js (v20+ recommended)
- npm

Install
Run from the repository root:
```sh
npm install
```

Run unit tests
- Run all tests (uses the package.json test script):
```sh
npm run test
```

Notes
- Run commands from the repository root.
- If tests fail, ensure dependencies are installed and any required environment variables are set.